# Log de decisões (append-only)

Cada entrada registra uma decisão relevante: o que foi decidido, por quê e quando.
Nunca apagar entradas antigas; apenas adicionar novas. Isso preserva o raciocínio por trás de cada escolha.

Formato de cada entrada:

```
## [YYYY-MM-DD] Título da decisão

**Decisão:** o que foi escolhido.
**Motivo:** por que esta opção e não outra.
**Contexto:** o que estava em jogo, se relevante.
```

---

_Nenhuma decisão registrada ainda. O /onboard e o uso diário do sistema vão popular este log._

---

## [2026-06-25] Portagem /templatar — lote de melhorias acumuladas

**Decisão:** portar 9 itens da fila MELHORIAS-PRO-TEMPLATE.md pro template limpo.
**O que entrou:**
- skill `workflows-ao-vivo` (nova): dashboard HTML para acompanhar workflows ao vivo, com abas Multi-agente / Custos / Scripts.
- skill `continuar-sessao` (atualizada): incorporado filtro anti-infra, roteamento por peso de sessão (>100k → delega ao tier-3), critério de pop-up por rajada (<5min).
- scripts `destilar-sessoes/narra.py` + `sintetiza.py` (novos): pipeline de narrativa por projeto e extração de conhecimento por conceito, com PROJETOS e caminhos generalizados via placeholders.
- `armadilhas-tecnicas.md` (atualizado): +9 seções (Python encoding/BOM, caminhos Windows, operações atômicas, CLI timeout, MCP servers, Playwright órfãos, semver, troca de modelo mid-session).
- regra Whisper GPU: sempre GPU, CPU é fallback — afeta qualquer skill de transcrição.
**Motivo:** acúmulo de ~30 sessões de produção desde a última portagem (v1.7.0). Entradas puramente técnicas e específicas de CSS/ffmpeg foram deixadas na fila (não portadas — muito situacionais pro template genérico).
